{
  "name": "softkvm-setup",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node --import tsx src/index.ts",
    "build": "bun build --outfile=dist/setup.mjs --target=node src/index.ts"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0"
  }
}
